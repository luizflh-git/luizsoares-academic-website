# assets/images/

Place site images here. Currently present:

- `favicon.svg` — placeholder monogram favicon ("LS"). Replace with a final version if desired (referenced in `_quarto.yml` as `website.favicon`).
- `imagem_foto_luiz_soares.png` — profile photo, used in the homepage hero (`index.qmd`, `.profile-photo` class in `styles.css`).

Still needed:

- **Open Graph / social preview image** (e.g. `og-image.png`, ~1200×630px) — for link previews on social platforms and Slack/Twitter cards. Add it and reference it via `website.open-graph.image` / `website.twitter-card.image` in `_quarto.yml` (see the [Quarto website metadata docs](https://quarto.org/docs/websites/website-tools.html)). The current profile photo is portrait-oriented and not a good fit for this landscape format — a separate image is recommended.
